import { Meta, Story } from '@storybook/addon-docs';
import {Box} from "@chakra-ui/react";
import Pagination from "./Pagination";
import { action } from '@storybook/addon-actions'

export const StoryComponent = ({ parentWidth, ...args }) => (<Box w={parentWidth}><Pagination {...args} /></Box>);

<Meta title="Pagination" />

# Pagination
The pagination component shows the visual controls of pagination, it's not responsible to tell if the user can paginate to previous or next page.

## On first Page

<Story name="Primary"
       args={{
         controller: {
           pageIndex: 0,
           pageSize: 20,
           pageCount: 3,
           hasNext: () => true,
           hasPrev: () => false,
           nextPage: () => action('nextPage')(),
           prevPage: () => action('prevPage')(),
           goToPage: (val) => action('goToPage')(val),
           setPageSize: (val) => action('setPageSize')(val),
         },
         parentWidth: '2xl'
       }}>
  {StoryComponent.bind({})}
</Story>


## On middle Page

<Story name="Middle Page"
       args={{
         controller: {
           pageIndex: 1,
           pageSize: 20,
           pageCount: 3,
           hasNext: () => true,
           hasPrev: () => true,
           nextPage: () => action('nextPage')(),
           prevPage: () => action('prevPage')(),
           goToPage: (val) => action('goToPage')(val),
           setPageSize: (val) => action('setPageSize')(val),
         },
         parentWidth: '2xl'
       }}>
  {StoryComponent.bind({})}
</Story>


## On last Page

<Story name="Last Page"
       args={{
         controller: {
           pageIndex: 2,
           pageSize: 20,
           pageCount: 3,
           hasNext: () => false,
           hasPrev: () => true,
           nextPage: () => action('nextPage')(),
           prevPage: () => action('prevPage')(),
           goToPage: (val) => action('goToPage')(val),
           setPageSize: (val) => action('setPageSize')(val),
         },
         parentWidth: '2xl'
       }}>
  {StoryComponent.bind({})}
</Story>
