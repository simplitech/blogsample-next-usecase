import { Meta, Story } from '@storybook/addon-docs';
import {Box} from "@chakra-ui/react";
import IndexHero from "./IndexHero";

export const StoryComponent = ({ parentWidth, ...args }) => (<Box w={parentWidth}><IndexHero {...args} /></Box>);

<Meta title="index/IndexHero" />

# IndexHero
An example of how the landingpage could be implemented.

<Story name="Primary"
  args={{
    parentWidth: '6xl'
  }}>
  {StoryComponent.bind({})}
</Story>
