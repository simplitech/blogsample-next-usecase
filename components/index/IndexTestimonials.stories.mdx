import { Meta, Story } from '@storybook/addon-docs';
import {Box} from "@chakra-ui/react";
import IndexTestimonials from "./IndexTestimonials";

export const StoryComponent = ({ parentWidth, ...args }) => (<Box w={parentWidth}><IndexTestimonials {...args} /></Box>);

<Meta title="index/IndexTestimonials" />

# IndexTestimonials
An example of how the landingpage could be implemented.

<Story name="Primary"
  args={{
    parentWidth: '6xl'
  }}>
  {StoryComponent.bind({})}
</Story>
