import { Meta, Story } from '@storybook/addon-docs';
import {Box} from "@chakra-ui/react";
import IndexPricing from "./IndexPricing";

export const StoryComponent = ({ parentWidth, ...args }) => (<Box w={parentWidth}><IndexPricing {...args} /></Box>);

<Meta title="index/IndexPricing" />

# IndexPricing
An example of how the landingpage could be implemented.

<Story name="Primary"
  args={{
    parentWidth: '6xl'
  }}>
  {StoryComponent.bind({})}
</Story>
