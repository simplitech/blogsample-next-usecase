import { Meta, Story } from '@storybook/addon-docs';
import {Box} from "@chakra-ui/react";
import BlogAuthor from "./BlogAuthor";

export const StoryComponent = ({ parentWidth, ...args }) => (<Box w={parentWidth}><BlogAuthor {...args} /></Box>);

<Meta title="blog/BlogAuthor" />

# BlogAuthor

## Primary
The BlogAuthor component is used to render information about the author and which time the post was created.

<Story name="Primary"
  args={{
    post: {
      createdAt: '2021-07-31T21:20:56.677Z',
      author: {
        name: 'John Albert Silva',
        avatarUrl: 'https://i.pravatar.cc/150?u=b'
      }
    },
    parentWidth: 'lg'
  }}>
  {StoryComponent.bind({})}
</Story>

## Placeholder

But it can also be used as a placeholder, simply doesnt pass any post to it.

<Story name="Placeholder"
       args={{
         parentWidth: 'lg'
       }}>
  {StoryComponent.bind({})}
</Story>
